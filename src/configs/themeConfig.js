/**
 * Config
 * -------------------------------------------------------------------------------------
 * Clear browser localStorage to pick up themeConfig defaults after changes.
 */
const themeConfig = {
  templateName: 'NetQwix',
  layout: 'vertical',
  mode: 'light',
  direction: 'ltr',
  skin: 'default',
  contentWidth: 'full',
  footer: 'static',
  routingLoader: true,
  navHidden: false,
  menuTextTruncate: true,
  navSubItemIcon: 'mdi:circle',
  verticalNavToggleType: 'accordion',
  navCollapsed: false,
  navigationSize: 260,
  collapsedNavigationSize: 68,
  afterVerticalNavMenuContentPosition: 'fixed',
  beforeVerticalNavMenuContentPosition: 'fixed',
  horizontalMenuToggle: 'hover',
  horizontalMenuAnimation: true,
  appBar: 'fixed',
  appBarBlur: false,
  responsiveFontSizes: true,
  disableRipple: true,
  disableCustomizer: true,
  toastPosition: 'top-right'
}

export default themeConfig
